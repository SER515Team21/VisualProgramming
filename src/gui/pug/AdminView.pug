include TabSwitcher

#AdminView(hidden='')
    include header
    .tab-switcher
        +tabbedFrame(['Courses','Teachers', 'Students'], ['Courses','Teachers', 'Students'])
        .view.active(id='Courses')
