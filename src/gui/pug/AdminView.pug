include GenericDashboard
include TabSwitcher
include ScrolledTable

#AdminView(hidden='')
    +GenericDashboard()
        +LeftPane()
            +tabbedFrame(['Courses', 'Teachers', 'Students'])
            .tab-switcher
                .view.active(id='Courses')
                    ul(id='globalCourseList')
                        li Empty
                        li Empty
                        li Empty
                        li Empty
                        li Empty
                        li Empty
                        li Empty
                        li Empty
                        li Empty
                        li Empty
        +MainPane()
            +tabbedFrame([])
            .tab-switcher
                .view.active(id='CourseEditor')
                    h1 Course Editor
                    h2(id='courseName') Course Name
                    #CourseEditorTeacher
                        h3 Teacher:
                        h3(id='teacherNameAdmin') Teacher Name
                        button(id='teacherNameEdit') Edit
                    +ScrolledTableBox('Students', 'adminStudentsEdit', ['Last Name', 'First Name', 'Student ID'],
                            [['Empty', 'Empty', 'Empty'], ['Empty', 'Empty', 'Empty'], ['Empty', 'Empty', 'Empty'],
                            ['Empty', 'Empty', 'Empty'], ['Empty', 'Empty', 'Empty'], ['Empty', 'Empty', 'Empty'],
                            ['Empty', 'Empty', 'Empty'], ['Empty', 'Empty', 'Empty'], ['Empty', 'Empty', 'Empty'],
                            ['Empty', 'Empty', 'Empty']])
